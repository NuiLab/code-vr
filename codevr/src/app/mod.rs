pub struct App {

}

impl App {
  pub fn new() -> App {
    App {}
  }

  fn update(&self) {
    /*
    // Inputmap has Iterator trait to match appropriate inptus.
    for input in self.inputmap {
      match input {
        ("move_right", x) => {
          self.hp += 1.;
        },
        ("move_down", y) => ()
      }
    }
    */
  }
}